node_modules/
dist/
coverage/
*.log
.elv/
.delegate/
.DS_Store
*.tsbuildinfo

# Local-only working docs: plans, reviews, research, audits
docs/internal/
# Agent tooling artifacts (root paths are tool contracts)
model-performance-journal.md
.papercuts.jsonl

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Root-level lock written by bd ready (see .beads/.gitignore for the rest)
/.beads.gate.lock
# Per-machine Claude Code settings and pool-skill links (claude-skill add)
.claude/settings.local.json
.claude/skills/cli-monthly-update
